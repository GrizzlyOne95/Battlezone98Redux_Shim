/*
 * Entry: 0044e910
 * Name: Strip::Strip
 * Namespace: Strip
 * Signature: Strip * Strip(Strip * this)
 * Symbol source: IMPORTED
 * Export status: ok
 */

Strip * __thiscall Strip::Strip(Strip *this)

{
  pathMem = pathMem + 0x3c;
  this->_padding_ = (int)&_vftable_;
  return this;
}
